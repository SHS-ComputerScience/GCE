""" Create a program in Python on Codio which asks the user to input their name and then prints the binary,
	denary and hexadecimal representation of their names. You may use the following built-in functions: bin(), ord() and hex().
	You may also use the ASCII table for reference.
"""
#============================================================
# name = input('Enter Your Name:')
# print('Your Name is',name)
# converted_name = ''
# # Converts Name To Binary Representation
# for i in range(len(name)):
#     den_converted = ord(name[i])
#     bin_converted = bin(den_converted)
#     binary = (bin_converted[2:] + ' ')
#     converted_name += binary
# print('Your Name in Binary is:',converted_name)

# converted_name = ''
# #Converts To Hexadecimal
# for i in range(len(name)):
#     den_converted = ord(name[i])
#     hex_converted = hex(den_converted)
#     hexadecimal = (hex_converted[2:] + ' ')
#     converted_name += hexadecimal
# print('Your Name in Hexadecimal is:',converted_name)
# ============================================================
# # name = input('Enter Your Name:')
# # print('Your Name is',name)
# # type_list = [bin,hex]
# # bin_list = ""
# 
# # for i in type_list:
# # 	for l in name:
# #         if len(v) > 4:
# #             den_converted = ord(name[i])
# #             bin_converted = bin(den_converted)
#             binary = (bin_converted[2:] + ' ')
# #             converted_name += binary
# #             print('Your Name in Binary is:',converted_name)
# #         else:
# #             den_converted = ord(name[i])
# #             hex_converted = hex(den_converted)
# #             hexadecimal = (hex_converted[2:] + ' ')
# #             converted_name += hexadecimal           
# #             print('Your Name in Hexadecimal is:',converted_name)
# 
		
